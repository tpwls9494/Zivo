"""편도 조합 최저가 알고리즘. 상세 구현은 Day 4. 지금은 자리표시자."""

from typing import Any


def build_combos(
    outbound: list[dict[str, Any]],
    inbound: list[dict[str, Any]],
    baseline_roundtrip_krw: int | None,
) -> list[dict[str, Any]]:
    """Day 4 에서 구현."""
    return []
